// GENERATED by scripts/sync.mjs — do not edit.
// Source of truth: algorithms/domains/D07-technical-indicators/D07-F04-volatility-and-channels/D07-F04-A05-donchian-channels/implementations/typescript/volatility_channels.ts
// Re-run `npm run sync` after changing the catalog implementation.

function finite(v:number[],n:string){if(!Array.isArray(v)||!v.length||v.some(x=>!Number.isFinite(x)))throw new Error(`${n} must contain finite numbers`)}
function period(v:number,n="period"){if(!Number.isInteger(v)||v<1)throw new Error(`${n} must be a positive integer`)}
export function donchianChannels(high:number[],low:number[],p=20){finite(high,"high");finite(low,"low");period(p);if(high.length!==low.length||high.some((h,i)=>h<low[i]))throw new Error("high and low must align with high >= low");const upper:(number|null)[]=Array(high.length).fill(null),lower:(number|null)[]=Array(high.length).fill(null),middle:(number|null)[]=Array(high.length).fill(null),width:(number|null)[]=Array(high.length).fill(null);for(let i=p-1;i<high.length;i++){upper[i]=Math.max(...high.slice(i-p+1,i+1));lower[i]=Math.min(...low.slice(i-p+1,i+1));middle[i]=((upper[i] as number)+(lower[i] as number))/2;width[i]=(upper[i] as number)-(lower[i] as number)}return{upper,lower,middle,width}}
